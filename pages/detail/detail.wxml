<view class="detail-wrapper">
  <image
    wx:for="{{ imgList }}"
    wx:for-item="item"
    wx:key="i"
    class="img-item"
    src="{{ item.url }}"
    data-src="{{ item.url }}"
    bindtap="preview"
  >
    <!-- <image src="{{ item.url }}"></image> -->
  </image>
</view>
